DOT_APP_API_TOKEN=
# 必須(list-devicesサブコマンドで確認できる対象デバイスのID)
DOT_DEVICE_ID=
# 省略時は30秒
MIN_PUSH_INTERVAL_SEC=
# 省略時は3件
EARTHQUAKE_ENTRY_COUNT=
# 任意(GlitchTip 等 Sentry 互換のエラートラッキングサービスの DSN。未設定時は無効)
SENTRY_DSN=
